@{
  Layout = "_Layout";
}

@model SweetSavory.Models.Treat

<h3>Add a Treat to the Database</h3>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)

  <input type="submit" value="Add Treat" />
}